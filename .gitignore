# Secrets & environment
.env
.infisical.json

# Runtime data (hidden working dirs — never commit)
.data/
.docs/

# IDE & tooling
.venv/
.claude/settings.json
.claude/settings.local.json
.vscode/

# Logs — runtime outputs, never commit
logs/

# Python
__pycache__/
**/__pycache__/
*.pyc
*.pyo

# OS
.DS_Store

# CV files (personal data)
query/resume/*.md
query/resume/*.pdf

# Hints cache (personal — auto-generated, commit if you want to share)
query/hints_cache.json

# Adaptive web search usage counters (runtime state, resets monthly)
query/search_usage.json

# OAuth tokens (auto-generated)
.oauth_client.json
.token.json

# Future phases
scoring_profiles/

# IT Team automation session data
.it-sessions/
